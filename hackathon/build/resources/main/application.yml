server:
  port: 8088

spring:
  profiles: default
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/seoulssudam?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: s0402
    #s0402

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      location: C:\ploggingImages

  output:
    ansi:
      enabled: always

jwt:
  issuer: "inky"
  secret_key: "seoulPlogging"

